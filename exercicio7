num_impar = int(input("Digite um número ímpar: "))
if num_impar % 2 == 0:
    print("O número precisa ser ímpar!")
else:
    anterior = num_impar - 2
    proximo = num_impar + 2
    diferenca = (proximo * 2) - (anterior * 2)
    print(f"A diferença entre os quadrados é: {diferenca}")
